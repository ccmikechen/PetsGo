import React from 'react';
import AdoptionInfo from '../../containers/AdoptionInfo';

const AdoptionInfoScreen = ({ navigator, animal }) => (
  <AdoptionInfo navigator={navigator} animal={animal} />
);

export default AdoptionInfoScreen;
