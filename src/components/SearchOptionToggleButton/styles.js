import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
  container: {
    flex: 1
  },
  toggleButton: {
    flex: 1,
    backgroundColor: '#FFF',
    justifyContent: 'center',
    alignItems: 'center'
  },
  icon: {
  },
  text: {
    fontSize: 10,
    color: '#555555',
    marginBottom: -7
  }
});
