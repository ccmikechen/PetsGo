import Immutable from 'immutable';

export const UiState = {

};

export const SearchState = {
  options: {
    hospital: {
      actived: false
    },
    restaurant: {
      actived: false
    },
    salon: {
      actived: false
    },
    necessities: {
      actived: false
    }
  }
};
